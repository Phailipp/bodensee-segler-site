# Bodensee Segler (Side project)

This folder contains the HTML prototype you sent via WhatsApp.

## What it is
A single-page website prototype (German) titled **"Bodensee Segler"** with:
- Hero section + styling
- Leaflet map integration
- Sections / layout (see `index.html`)

## Next sensible steps
- Decide hosting target (static: GitHub Pages, Netlify, Vercel, simple webspace)
- Extract assets/config (images, map pins, boat data) into JSON
- Split into `/css` + `/js` if it grows

(Generated by Max)
